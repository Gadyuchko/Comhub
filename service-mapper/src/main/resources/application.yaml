spring:
  application:
    name: service-mapper
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    consumer:
      auto-offset-reset: earliest

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus

kafka:
  group:
    config-replay: service-mapper-config-${HOSTNAME:local-dev}
  topics:
    config-replay: comhub.config.v1
    canonical: comhub.canonical.v1
    dlq: comhub.dlq.v1
  source:
    consumer:
      group-prefix: comhub.mapper
  config-replay:
    startup-timeout: 30s
